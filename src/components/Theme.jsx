import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#ff6b0a',
      accent: '#ef6306',
      lightText: '#fff9f2',
      darkText: '#464444',
    },
    secondary: {
      main: '#ff8f27',
    },
    disable: {
      main: '#efb27a',
    },
  },
});
