import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#fe7d15',
      light: '#fe9845',
      lightText: '#fff9f2',
      darkText: '#464444',
    },
    secondary: {
      main: '#f8a058',
    },
  },
});
