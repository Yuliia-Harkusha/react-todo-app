import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#fe7d15',
      light: '#f8a058',
      lightText: '#fbeedf',
      darkText: '#464444',
    },
    secondary: {
      main: '#ff9800',
    },
  },
});
